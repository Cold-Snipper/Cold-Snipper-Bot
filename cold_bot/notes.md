## Deployment Notes

- Start Ollama server before running the bot.
- Set environment variables in `.env` and keep credentials out of source control.
- Use public data only; respect site ToS and rate limits.
- Run: `python main.py --config config.yaml`
